greetings = {
	### Hostile Greetings
	01_INFESTER_HOSTILE_GREETING = {
		weight = {
			weight = 100
		}
		
		trigger = {
			has_valid_civic = civic_infestation
			FROM = {
				is_country_type = default	
				NOT = { 		
					has_authority = auth_machine_intelligence
				}
			}
			OR = {
				is_hostile_to = FROM
				is_domineering_to = FROM
				is_unfriendly_to = FROM
			}
		}
	}
	
	### Neutral Greetings
	01_INFESTER_NEUTRAL_GREETING = {
		weight = {
			weight = 100
		}
		
		trigger = {
			has_valid_civic = civic_infestation
			FROM = {
				is_country_type = default	
				NOT = { 		
					has_authority = auth_machine_intelligence
				}
			}
			is_neutral_to = FROM
		}
	}
	
	### Friendly Greetings
	01_INFESTER_FRIENDLY_GREETING = {
		weight = {
			weight = 100
		}
		
		trigger = {
			has_valid_civic = civic_infestation
			FROM = {
				is_country_type = default	
				NOT = { 		
					has_authority = auth_machine_intelligence
				}
			}
			OR = {
				is_friendly_to = FROM
				is_cordial_to = FROM
				is_protective_to = FROM
			}
		}
	}
}