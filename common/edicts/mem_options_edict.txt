mem_options_menu = {
	icon = "GFX_edict_type_time"
	length = 0
	modifier = {
	}
	
	resources = {
		category = edicts
		cost = {
			influence = 0
		}
	}

	potential = {
		has_country_flag = mem_host
		# hide the edict if DMM is installed
		hidden_trigger = {
			NOT = {
				has_global_flag = dmm_installed
			}
		}
	}
	
	effect = {
		country_event = { id = mem_options.4 }
	}

	allow = {
		has_country_flag = mem_host
		hidden_trigger = {
			NOT = {
				has_global_flag = dmm_installed
			}
		}
	}

	ai_weight = {
		weight = 0
	}	
}