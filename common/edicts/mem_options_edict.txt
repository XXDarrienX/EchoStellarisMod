mem_options_menu = {
	length = 0
	modifier = {
	}
	
	resources = {
		category = edicts
		cost = {
			influence = 0
		}
	}

	potential = {
		exists = owner
		OR = {
			is_multiplayer = yes
			owner = {
				is_ai = no
				has_country_flag = game_admiin
			}
		}
		OR = {
			is_multiplayer = no
			owner = {
				is_ai = no
			}
		}
	}
	
	effect = {
		country_event = { id = mem_options.4 }
	}

	allow = {
	}

	ai_weight = {
		weight = 0
	}	
}