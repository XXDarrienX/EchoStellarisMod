mem_options_menu = {
	icon = "GFX_edict_type_time"
	length = 0
	modifier = {
	}
	
	resources = {
		category = edicts
		cost = {
			influence = 0
		}
	}

	potential = {
		OR = {
			is_multiplayer = no
			has_country_flag = mem_host
		}
	}
	
	effect = {
		country_event = { id = mem_options.4 }
	}

	allow = {
		OR = {
			is_multiplayer = no
			has_country_flag = mem_host
		}
	}

	ai_weight = {
		weight = 0
	}	
}