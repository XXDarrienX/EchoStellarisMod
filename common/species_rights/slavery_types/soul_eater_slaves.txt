# from = country
# this = species

slavery_soul_eater = {
	pop_escape_chance = 0.01
	pop_modifier = {
		pop_happiness = -0.25
		planet_jobs_produces_mult = -0.15
		planet_pops_soul_crystals_produces_mult = 0.15
		pop_defense_armies_add = 1
	}

	pop_tooltip = SOUL_EATER_LIVESTOCK
	pop_status_frame = 6

	potential = {
		host_has_dlc = "Utopia"
		from = { NOT = { has_authority = auth_machine_intelligence } }
		from = { has_origin = origin_vb_psi_soul_eater }
	}

	allow = {
		hidden_trigger = { exists = from }
		custom_tooltip = {
			fail_text = CANNOT_LIVESTOCK_MATRIX_MACHINES
			NOR = {
				has_trait = trait_mechanical
				has_trait = trait_machine_unit
			}
		}
	}
	ai_will_do = {
		factor = 100
	}
}