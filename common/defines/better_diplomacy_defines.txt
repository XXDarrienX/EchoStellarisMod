NGameplay = {
	ENFORCE_STATUS_QUO_MONTHS			  	= 48		# (24)

	PLANET_HEALTH_REGEN						= 0.04		# (0.05) 	Each day
	PLANET_HEALTH_REGEN_DELAY 				= 120		# (60) 		Days since being bombarded before repairs can be done at full speed

	FEDERATION_JOIN_COHESION_PENALTY	= -35			# Cohesion loss every time a new member is added to the federation (after the first two) #(was -100)
	FEDERATION_LEAVE_COHESION_PENALTY	= -35 			# Cohesion loss every time a member leaves the federation #(was -100)
	FEDERATION_SWITCH_COHESION_PENALTY	= -100			# Cohesion loss when you switch federation type #(was -50)

	ENVOY_RELATION_EFFECT_DECAY			= 0.25			# Every month that there is no envoy effecting opinion it moves towards 0 with this/month (was 1.0)

	DIPLOMACY_WEIGHT_NAVAL_FACTOR			= 0.020		# (0.025)	Factors Naval Power before it is added to diplo weight
	DIPLOMACY_WEIGHT_GESTALT_WEIGHT			= 0.015		# (0.01)		Gestalts gets a flat base value

	OCCUPATION_BASE_VALUE					= 2.5		# Base value of occupied system, used for calculating relative occupation score of empire (was 5)
	OCCUPATION_PLANET_VALUE					= 7.5		# Per planet in system (was 10)
	OCCUPATION_POP_VALUE					= 0.002		# Per pop in system (was 0.001)

	FEDERATION_VOTE_RESUBMIT_DELAY	= 720				# Number of days that must pass before you can propose to vote for a given law again (was 180)

	FIRST_CONTACT_DISCOVERY_DAYS		= 100			# (80)		50 days for 1 roll 
	FIRST_CONTACT_DISCOVERY_DAYS_AI		= 125			# (110)		slower for AI to guarantee player has a chance to finish first

	SPY_NETWORK_SPRAWL_SCORE_SCALE		= 100			# (400)		This many empire sprawl in the opposing empire adds 1 to daily spy network progression.

	AGREEMENT_CHANGE_COOLDOWN_MONTHS 			= 30	# How often an agreement can be changed in months (was 60)
	AGREEMENT_LOYALTY_DELTA_MULT				= 2.5	# Used in Influence Cost calculations (was 5)
	AGREEMENT_PROPOSE_INFLUENCE_COST 			= 25	# Base cost of agreement proposal. + |Loyalty Delta| * AGREEMENT_LOYALTY_DELTA_MULT between old and new agreements (was 50)
	AGREEMENT_DENY_AS_SUBJECT_INFLUENCE_COST	= 75	# + |Loyalty Delta| * AGREEMENT_LOYALTY_DELTA_MULT between old and new agreements (was 50)
	AGREEMENT_DENY_AS_OVERLORD_INFLUENCE_COST	= 15	# + |Loyalty Delta| * AGREEMENT_LOYALTY_DELTA_MULT between old and new agreements (was 25)
	
	# Grand Archive collection
	SELL_SPECIMEN_ENERGY_VALUES = {
		500		#Common
		1000	#Rare
		2000	#Epic
		4000	#Exceptional
	}
	TRADE_VALUE_SPECIMEN_MULT = 2.0	# Specimen Trade Value (in Energy) = Sell Value * TRADE_VALUE_SPECIMEN_MULT
	TRADE_VALUE_SPECIMEN_COMPLETE_CATEGORY_MULT = 1.5
	TRADE_VALUE_SPECIMEN_EMPTY_CATEGORY_MULT = 2.5
}

NAI = {
	VASSALIZATION_ACCEPTANCE_NUM_POPS = -0.001			# per pop (was -0.0025)
	VASSALIZATION_ACCEPTANCE_POWER_DIFFERENCE_MAX = 500	# (was 100)

	TRADE_VALUE_SYSTEM = 100 							# (was 5)
	TRADE_VALUE_SENSOR_CHARTS = 5						# Per new comms (was 2)
	TRADE_VALUE_LOYALTY = 10.0							# Multiplied by the Loyalty offered in the trade (was 1.0)

	PEACE_RELATIVE_NAVY_STRENGTH_FACTOR = 75		# (50)
	
	SECRET_FEALTY_PLEDGE_TERMS_MULTIPLIER = 25 # (14) # Multiplied with the disloyalty of a subject. If this is higher, the AI subject is more likely to pledge Secret Fealty
	SECRET_FEALTY_PROPOSE_TERMS_MULTIPLIER = 25 # (14) # Multiplied with the disloyalty of a subject. If this is higher, the AI is more likely to propose a subject to pledge Secret Fealty
}