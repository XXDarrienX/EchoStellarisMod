# triggered district flavor will pick the first one that they hit.

# zone_energy_physics
triggered_flavor_desc = {
	trigger = {
        num_zones = {
			type = zone_energy_physics
			value > 0
		}
	}
	text = zone_energy_physics_desc
}

inline_script = {
	script = districts/district_triggered_flavor_desc_generator_default
}
