if = {
    limit = {
        has_cycle_of_$CYCLE$ = no
    }

    clear_cycle_flags = yes
    set_timed_country_flag = {
        flag = cycle_of_$CYCLE$
        years = @sb_cycles_duration_years
    }
    set_timed_country_flag = {
        flag = current_cycle
        years = @sb_cycles_duration_years
    }
}

break_cycles = yes

add_modifier = {
    modifier = cycle_of_$CYCLE$_positive
    mult = value:cycle_positive_effects_mult_value
    days = 1
    time_multiplier = value:current_cycle_days_left
}
add_modifier = {
    modifier = cycle_of_$CYCLE$
    mult = value:cycle_negative_effects_mult_value
    days = 1
    time_multiplier = value:current_cycle_days_left
}
