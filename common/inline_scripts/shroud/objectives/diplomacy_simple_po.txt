country_event = {
	id = $EVENT_ID$
	hide_window = yes
	is_triggered_only = yes

	trigger = {
		exists = from
		NOT = { has_country_flag = $FLAG_NAME$_@from }
	}

	immediate = {
		set_country_flag = $FLAG_NAME$_@from
		if = {
			limit = { can_access_shroud = yes }
			add_patron_objective_counter = {
				counter = $PATRON_OBJECTIVE$
				amount = 1
			}
		}
	}
}
