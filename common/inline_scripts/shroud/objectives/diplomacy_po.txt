country_event = {
	id = $EVENT_ID$
	hide_window = yes
	is_triggered_only = yes

	trigger = {
		exists = from
		NOR = {
			has_country_flag = $FLAG_NAME$_@from
			from = { has_country_flag = $FLAG_NAME$_@root }
		}
	}

	immediate = {
		set_country_flag = $FLAG_NAME$_@from
		if = {
			limit = { can_access_shroud = yes }
			add_patron_objective_counter = {
				counter = $PATRON_OBJECTIVE$
				amount = 1
			}
		}
		from = {
			set_country_flag = $FLAG_NAME$_@root
			if = {
				limit = { can_access_shroud = yes }
				add_patron_objective_counter = {
					counter = $PATRON_OBJECTIVE$
					amount = 1
				}
			}
		}
	}
}
