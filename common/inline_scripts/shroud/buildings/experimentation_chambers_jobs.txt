triggered_planet_modifier = {
	potential = {
		OR = {
			is_scope_valid = no
			AND = {
				exists = owner
				owner = {
					is_gestalt = no
				}
			}
		}
	}
	modifier = {
		job_test_subject_add = $TEST_SUBJECT_JOBS$
		job_experiment_engineer_add = $EXPERIMENTER_JOBS$
	}
}

triggered_planet_modifier = {
	potential = {
		exists = owner
		owner = {
			is_gestalt = yes
		}
	}
	modifier = {
		job_test_subject_drone_add = $TEST_SUBJECT_JOBS$
		job_experiment_engineer_drone_add = $EXPERIMENTER_JOBS$
	}
}

triggered_planet_modifier = {
	potential = {
		has_modifier = penal_colony
		exists = owner
		owner = {
			is_gestalt = no
		}
	}
	modifier = {
		job_test_subject_add = $PENAL_JOBS$
	}
}

triggered_planet_modifier = {
	potential = {
		has_modifier = penal_colony
		exists = owner
		owner = {
			is_gestalt = yes
		}
	}
	modifier = {
		job_test_subject_drone_add = $PENAL_JOBS$
	}
}
