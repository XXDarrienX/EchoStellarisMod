option = {
	name = $EVENT_ID$.a.$CATEGORY$
	trigger = { has_received_every_$CATEGORY$_reward = no }
	allow = {
		custom_tooltip = {
			fail_text = $EVENT_ID$.tooltip.no_pending_reward
			OR = {
				is_eligible_for_collection_stage_1_reward_$CATEGORY$ = yes
				is_eligible_for_collection_stage_2_reward_$CATEGORY$ = yes
				is_eligible_for_collection_stage_3_reward_$CATEGORY$ = yes
			}
		}
	}
	hidden_effect = {
		country_event = { id = $FOLLOWUP_EVENT_ID$ }
	}
}
option = {
	name = $EVENT_ID$.a.$CATEGORY$
	trigger = { has_received_every_$CATEGORY$_reward = yes }
	allow = {
		custom_tooltip = {
			fail_text = $EVENT_ID$.tooltip.no_reward.$CATEGORY$
			always = no
		}
	}
}
