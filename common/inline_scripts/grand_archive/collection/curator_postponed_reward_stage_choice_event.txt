country_event = {
	id = $EVENT_ID$
	base = leviathans.500
	desc_clear = yes
	option_clear = yes

	desc = $LOCALIZATION_ID$.desc

	option = {
		name = $LOCALIZATION_ID$.a
		trigger = { NOT = { has_country_flag = received_collection_stage_1_reward_$CATEGORY$ } }
		allow = {
			custom_tooltip = {
				fail_text = REWARD_NOT_AVAILABLE
				is_eligible_for_collection_stage_1_reward_$CATEGORY$ = yes
			}
		}
		hidden_effect = {
			country_event = { id = $STAGE_1_EVENT_ID$ }
		}
	}

	option = {
		name = $LOCALIZATION_ID$.b.$CATEGORY$
		trigger = { NOT = { has_country_flag = received_collection_stage_2_reward_$CATEGORY$ } }
		allow = {
			custom_tooltip = {
				fail_text = REWARD_NOT_AVAILABLE
				is_eligible_for_collection_stage_2_reward_$CATEGORY$ = yes
			}
			has_resource = { type = energy amount >= @curator_stage_2_price }
		}
		add_resource = { energy = @curator_stage_2_price mult = -1 }
		give_collection_stage_2_reward_$CATEGORY$ = yes
	}

	option = {
		name = $LOCALIZATION_ID$.c
		trigger = { NOT = { has_country_flag = received_collection_stage_3_reward_$CATEGORY$ } }
		allow = {
			custom_tooltip = {
				fail_text = REWARD_NOT_AVAILABLE
				is_eligible_for_collection_stage_3_reward_$CATEGORY$ = yes
			}
		}
		hidden_effect = {
			country_event = { id = $STAGE_3_EVENT_ID$ }
		}
		ai_chance = {
			modifier = {
				factor = 0
				mid_game_years_passed < 20
			}
		}
	}

	option = {
		name = leviathans.506.g
		hidden_effect = {
			country_event = { id = leviathans.4100 }
		}
	}

	# Back
	option = {
		name = leviathans.506.d
		hidden_effect = {
			country_event = { id = leviathans.500 }
		}
	}

	# Goodbye
	option = {
		name = GOODBYE
		default_hide_option = yes
	}
}
