variable = {
	type = key
	localization = $RESOURCE$_icon
	value = $RESOURCE$_icon
	scope = this
	trigger = {
		check_variable = { which = trophy_hunter_gained_$RESOURCE$ value > 0 }
	}
}
variable = {
	type = variable
	localization = $RESOURCE$
	varname = this.trophy_hunter_gained_$RESOURCE$
	scope = this
	trigger = {
		check_variable = { which = trophy_hunter_gained_$RESOURCE$ value > 0 }
	}
}