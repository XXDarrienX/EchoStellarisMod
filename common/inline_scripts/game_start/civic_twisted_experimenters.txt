# Experimental Sentencing Inital Setup
# Buildings Added:
#	* Experimentation Chambers

if = {
	limit = {
		exists = owner
		owner = {
			is_twisted_experimenters_empire = yes
		}
	}
	if = {
		limit = {
			has_zone = {
				zone = zone_habitat_research_unity
			}
		}
		add_building = {
			zone = zone_habitat_research_unity
			building = building_experimentation_chambers_1
		}
	}
	else_if = {
		limit = {
			exists = owner
			owner = {
				has_origin = origin_cybernetic_creed
			}
		}
		add_building = building_experimentation_chambers_1
	}
	else = {
		add_building = {
			zone = zone_research_unity
			building = building_experimentation_chambers_1
		}
	}
}