
# Materialist Ethics Initial Setup
# Buildings Added:
#	* Research Labs

if = {
	limit = {
		exists = owner
		owner = {
			is_materialist = yes
		}
	}
	if = {
		limit = {
			has_zone = {
				zone = zone_research_unity
			}
		}
		add_building = {
			zone = zone_research_unity
			building = building_research_lab_1
		}
	}
	else_if = {
		limit = {
			has_zone = {
				zone = zone_habitat_research_unity
			}
		}
		add_building = {
			zone = zone_habitat_research_unity
			building = building_research_lab_1
		}
	}
	else = {
		add_building = {
			building = building_research_lab_1
		}
	}
}
