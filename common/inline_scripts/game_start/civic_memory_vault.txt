# Vaults of Knowledge Inital Setup
# Buildings Added:
#	* Vaults of Knowledge

if = {
	limit = {
		exists = owner
		owner = {
			is_memory_vault_empire = yes
		}
	}
	add_building = building_paragon_memory_vaults
	owner = {
		set_variable = {
			which = paragon_memory_vault
			value = 0
		}
	}
}
