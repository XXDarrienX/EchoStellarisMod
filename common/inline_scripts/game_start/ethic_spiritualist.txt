# Spiritualist Ethics Initial Setup
# Buildings Added:
#	* Temple

if = {
	limit = {
		has_zone = {
			zone = zone_habitat_research_unity
		}
		exists = owner
		owner = {
			is_spiritualist = yes
		}
	}
	add_building = {
		zone = zone_habitat_research_unity
		building = building_temple
	}
}
else_if = {
	limit = {
		exists = owner
		owner = {
			is_spiritualist = yes
		}
	}
	add_building = {
		zone = zone_research_unity
		building = building_temple
	}
}
