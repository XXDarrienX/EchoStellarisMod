triggered_planet_pop_group_modifier_for_species = {
    potential = {
        has_infertile_clone_soldier_trait = yes
        planet = {
            check_variable = { which = clone_pops_missing_per_vat value <= $UPPER_LIMIT$ }
            check_variable = { which = clone_pops_missing_per_vat value > $LOWER_LIMIT$ }
        }
    }
    bonus_pop_growth = $GROWTH$
}
triggered_planet_modifier = {
    potential = {
        check_variable = { which = clone_pops_missing_per_vat value <= $UPPER_LIMIT$ }
        check_variable = { which = clone_pops_missing_per_vat value > $LOWER_LIMIT$ }
    }
    fake_pop_growth_mod = $GROWTH$
}
