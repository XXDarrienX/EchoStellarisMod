vb_psi_cb_soul_eater = {
	show_in_diplomacy = yes
	show_notification = yes
	potential = {
		has_valid_civic = civic_vb_psi_soul_eater	
		}
	is_valid = {
		has_total_war_cb = no
		OR = {
			is_subject = no
			overlord = { NOT = { is_same_value = from } }
		}
	}
}

vb_cb_containment_soul_eater = {
	show_in_diplomacy = yes
	show_notification = yes
	potential = {
		is_country_type = default	
		NOT = {
			has_valid_civic = civic_vb_psi_soul_eater
		}
	}
	is_valid = {
		has_total_war_cb = no
		OR = {
			is_subject = no
			overlord = { NOT = { is_same_value = from } }
		}
		from = {
			has_valid_civic = civic_vb_psi_soul_eater
		}		
	}
}