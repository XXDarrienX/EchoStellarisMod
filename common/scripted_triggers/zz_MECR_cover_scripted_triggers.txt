is_assigned_to_galcom_any_job =  {
	OR = {
		has_envoy_task = { task = galactic_community }
		has_envoy_task = { task = strengthen_imperial_authority }
		has_envoy_task = { task = undermine_imperial_authority }
		has_leader_flag = flag_is_assigned_to_galcom
	}
}
is_assigned_to_federation =  {
	OR = {
		has_envoy_task = { task = federation }
		AND = { # vanilla bug fix
			leader_class = official
			is_idle = no
			is_gestalt_node = no
			NOR = {
				exists = planet
				has_envoy_task = { task = galactic_community }
				has_envoy_task = { task = strengthen_imperial_authority }
				has_envoy_task = { task = undermine_imperial_authority }
			}
		}
	}
}

is_MECR_activated = { always = yes }

is_unrecognized_cosmogenesis = {
	has_ascension_perk = ap_cosmogenesis
	NOT = { has_country_flag = flag_recognized_cosmogenesis }
}
is_recognized_cosmogenesis_empire = {
	has_ascension_perk = ap_cosmogenesis
	has_country_flag = flag_recognized_cosmogenesis
}