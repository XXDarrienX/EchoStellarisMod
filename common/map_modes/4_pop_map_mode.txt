pop_map_mode = {
	icon = "GFX_map_mode_pop"
	enable_terra_incognita = yes
	color = {
		value = hsv { 0.1 1.0 1.0 1.0 }
		custom_tooltip = "Up to 25 Pops in [This.GetName]"
			condition = {
			  num_pops >= 0
			  num_pops <= 25
			}
	}
	color = {
		value = hsv { 0.15 1.0 1.0 1.0 }
		custom_tooltip = "Up to 50 Pops in [This.GetName]"
			condition = {
			  num_pops >= 26
			  num_pops <= 50
			}
	}
	color = {
		value = hsv { 0.2 1.0 1.0 1.0 }
		custom_tooltip = "Up to 75 Pops in [This.GetName]"
			condition = {
			  num_pops >= 51
			  num_pops <= 75
			}
	}
	color = {
		value = hsv { 0.25 1.0 1.0 1.0 }
		custom_tooltip = "Up to 100 Pops in [This.GetName]"
			condition = {
			  num_pops >= 76
			  num_pops <= 100
			}
	}
	color = {
		value = hsv { 0.3 1.0 0.6 1.0 }
		custom_tooltip = "Up to 125 Pops in [This.GetName]"
			condition = {
			  num_pops >= 101
			  num_pops <= 125
			}
	}
	color = {
		value = hsv { 0.35 1.0 0.6 1.0 }
		custom_tooltip = "Up to 150 Pops in [This.GetName]"
			condition = {
			  num_pops >= 126
			  num_pops <= 150
			}
	}
	color = {
		value = hsv { 0.40 1.0 0.6 1.0 }
		custom_tooltip = "Up to 175 Pops in [This.GetName]"
			condition = {
			  num_pops >= 151
			  num_pops <= 175
			}
	}
	color = {
		value = hsv { 0.45 1.0 0.6 1.0 }
		custom_tooltip = "Up to 200 Pops in [This.GetName]"
			condition = {
			  num_pops >= 176
			  num_pops <= 200
			}
	}		
	color = {
		value = hsv { 0.5 1.0 0.6 1.0 }
		custom_tooltip = "Up to 225 Pops in [This.GetName]"
			condition = {
			  num_pops >= 201
			  num_pops <= 225
			}
	}
	color = {
		value = hsv { 0.55 1.0 0.6 1.0 }
		custom_tooltip = "Up to 250 Pops in [This.GetName]"
			condition = {
			  num_pops >= 226
			  num_pops <= 250
			}
	}
	color = {
		value = hsv { 0.6 1.0 0.6 1.0 }
		custom_tooltip = "Up to 275 Pops in [This.GetName]"
			condition = {
			  num_pops >= 251
			  num_pops <= 275
			}
	}
	color = {
		value = hsv { 0.65 1.0 0.6 1.0 }
		custom_tooltip = "Up to 300 Pops in [This.GetName]"
			condition = {
			  num_pops >= 276
			  num_pops <= 300
			}
	}
	color = {
		value = hsv { 0.7 1.0 0.6 1.0 }
		custom_tooltip = "Up to 325 Pops in [This.GetName]"
			condition = {
			  num_pops >= 301
			  num_pops <= 325
			}
	}
	color = {
		value = hsv { 0.75 1.0 0.6 1.0 }
		custom_tooltip = "Up to 350 Pops in [This.GetName]"
			condition = {
			  num_pops >= 326
			  num_pops <= 350
			}
	}
	color = {
		value = hsv { 0.8 1.0 0.6 1.0 }
		custom_tooltip = "Up to 375 Pops in [This.GetName]"
			condition = {
			  num_pops >= 351
			  num_pops <= 375
			}
	}
	color = {
		value = hsv { 0.85 1.0 0.6 1.0 }
		custom_tooltip = "Up to 400 Pops in [This.GetName]"
			condition = {
			  num_pops >= 376
			  num_pops <= 400
			}
	}
	color = {
		value = hsv { 0.90 1.0 0.6 1.0 }
		custom_tooltip = "Up to 500 Pops in [This.GetName]"
			condition = {
			  num_pops >= 401
			  num_pops <= 500
			}
	}
	color = {
		value = hsv { 0.95 1.0 0.6 1.0 }
		custom_tooltip = "Up to 750 Pops in [This.GetName]"
			condition = {
			  num_pops >= 501
			  num_pops <= 750
			}
	}
	color = {
		value = hsv { 0.99 1.0 0.6 1.0 }
		custom_tooltip = "Proverbially ∞ Pops in [This.GetName]"
			condition = {
			  num_pops <= 751
			  			}
	}
}