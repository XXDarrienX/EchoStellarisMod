quarian_system = {
	class = sc_neutron_star
	asteroid_belt = {
		type = rocky_asteroid_belt
		radius = 90
	}
	
	usage = custom_empire
	flags = { empire_home_system quarian_system }
	
	init_effect = {
		every_neighbor_system = {
			set_star_flag = empire_cluster
			every_neighbor_system = {
				set_star_flag = empire_cluster
			}
		}
		generate_home_system_resources = yes
	}
	
	planet = {
		class = star
		size = 90
		orbit_distance = 0
	}
	
	planet = {
		class = random_asteroid
		orbit_distance = 40
		size = 1
		has_ring = no
			init_effect = {
			orbital_deposit_tile = {
					clear_deposits = yes
					add_deposit = d_mineral_deposit 
					}
				}
	}
	planet = {
		class = random_asteroid
		orbit_distance = 20
		size = 5
		has_ring = no
			init_effect = {
			orbital_deposit_tile = {
					clear_deposits = yes
					add_deposit = d_mineral_deposit 
					}
				}
	}
	planet = {
		class = random_asteroid
		orbit_distance = 30
		size = 3
		has_ring = no
			init_effect = {
			orbital_deposit_tile = {
					clear_deposits = yes
					add_deposit = d_mineral_deposit 
					}
				}
	}
	planet = {
		class = random_asteroid
		orbit_distance = 30
		size = 9
		has_ring = no
			init_effect = {
			orbital_deposit_tile = {
					clear_deposits = yes
					add_deposit = d_mineral_deposit 
					}
				}
	}
	planet = {
		class = random_asteroid
		orbit_distance = 30
		size = 13
		has_ring = no
			init_effect = {
			orbital_deposit_tile = {
					clear_deposits = yes
					add_deposit = d_mineral_deposit 
					}
				}
	}
	planet = {
		class = random_asteroid
		orbit_distance = 30
		size = 11
		has_ring = no
			init_effect = {
			orbital_deposit_tile = {
					clear_deposits = yes
					add_deposit = d_mineral_deposit 
					}
				}
	}
	planet = {
		class = random_asteroid
		orbit_distance = 35
		size = 9
		has_ring = no
			init_effect = {
			orbital_deposit_tile = {
					clear_deposits = yes
					add_deposit = d_mineral_deposit 
					}
				}
	}
	planet = {
		class = random_asteroid
		orbit_distance = 35
		size = 5
		has_ring = no
			init_effect = {
			orbital_deposit_tile = {
					clear_deposits = yes
					add_deposit = d_mineral_deposit 
					}
				}
	}
	planet = {
		class = random_asteroid
		orbit_distance = 35
		size = 16
		has_ring = no
			init_effect = {
			orbital_deposit_tile = {
					clear_deposits = yes
					add_deposit = d_mineral_deposit 
					}
				}
	}
	planet = {
		class = random_asteroid
		orbit_distance = 35
		size = 20
		has_ring = no
			init_effect = {
			orbital_deposit_tile = {
					clear_deposits = yes
					add_deposit = d_mineral_deposit 
					}
				}
	}
	planet = {
		class = "pc_asteroid"
		orbit_distance = 40
		size = 5
		has_ring = no
			init_effect = {
			orbital_deposit_tile = {
					clear_deposits = yes
					add_deposit = d_mineral_deposit 
					}
				}
	}
	planet = {
			name = "Erykian"
			class = "pc_arid"
			orbit_distance = 85
			orbit_angle = 20
			size = 25
			starting_planet = yes
			has_ring = yes
			tile_blockers = none
			modifiers = none
  		
		init_effect = {
			prevent_anomaly = yes
			generate_start_buildings_and_blockers = yes
		}
	  }
}
