planet_jobs = {
    icon = "dummy"
    parent = planets

    triggered_produces_modifier = { # results in planet_jobs_slave_produces_add/mult modifiers that affect all job output from pops that match the trigger
        key = planet_jobs_undesirable
        trigger = {
            is_pop_category = undesirable
        }
    }
}