namespace = photo

#Mod Flag
event = {
	id = photo.0
	hide_window = yes	
	is_triggered_only = yes

	trigger = {
		NOT = { has_global_flag = photosynthesis_active }		
	}
	immediate = {
		set_global_flag = photosynthesis_active
	}
}