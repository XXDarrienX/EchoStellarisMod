namespace = echo_temp

country_event = {
    id = echo_temp.1
    hide_window = yes
    is_triggered_only = yes

    immediate = {
        every_playable_country = {
            limit = {
                has_tradition = tr_plentiful_faith_finish
            }
            add_modifier = {
                modifier = echo_temp_modifier
                days = 90
            }
        }
    }
}