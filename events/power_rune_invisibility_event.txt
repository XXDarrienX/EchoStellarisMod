namespace = invisibility_rune


country_event = {
	id = invisibility_rune.1
	hide_window = yes
	is_triggered_only = yes

	immediate = {
		every_system_within_border = {
			limit = {
				exists = owner
				owner = { has_relic = r_invisibility_rune }
			}
			add_modifier = {
				modifier = invisibility_rune
				days = 3600
			}
		}
	}
}