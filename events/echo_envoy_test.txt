namespace = envoy_test

country_event = {
	id = envoy_test.1
	hide_window = yes
	is_triggered_only = yes

	immediate = {
		every_playable_country = {
			limit = {
				any_relation = {
					is_improving_relations_with = from
					any_envoy = {
						has_envoy_task = {
							task = improve_relations
							target = from
						}
					}
				}
			}
			add_modifier = {
				modifier = nsc_military_advisor
				days = -1
			}
		}
	}
}