namespace = addtrait_leader

country_event = {
	id = addtrait_leader.1
	is_triggered_only = yes
	hide_window = yes

	trigger = {
			from.species = { has_trait = trait_additrait_hateful }
	}
	
	immediate = {
		from = {
		switch = {
			trigger = leader_class
			ruler = {
				add_ruler_trait = trait_additrait_hateful_rul
			}
			scientist = {
				add_ruler_trait = trait_additrait_hateful_rul
			}
			governor = {
				add_ruler_trait = trait_additrait_hateful_rul
			}
			general = {
				add_ruler_trait = trait_additrait_hateful_rul
			}
			admiral = {
				add_ruler_trait = trait_additrait_hateful_rul
				}
			}
		}
	}
}

country_event = {
	id = addtrait_leader.2
	is_triggered_only = yes
	hide_window = yes

	trigger = {
			from.species = { has_trait = trait_additrait_born_warriors }
	}

	immediate = {
		from = {
		switch = {
			trigger = leader_class
			admiral = {
				add_trait = trait_additrait_born_warriors_adm
			}
			general = {
				add_trait = trait_additrait_born_warriors_gen
				}
			}
		}
	}
}

country_event = {
	id = addtrait_leader.4
	is_triggered_only = yes
	hide_window = yes

	trigger = {
			from.species = { has_trait = trait_additrait_belligerent }
	}
	immediate = {
		from = {
		switch = {
			trigger = leader_class
			ruler = {
				add_ruler_trait = trait_additrait_belligerent_rul
			}
			scientist = {
				add_ruler_trait = trait_additrait_belligerent_rul
			}
			governor = {
				add_ruler_trait = trait_additrait_belligerent_rul
			}
			general = {
				add_ruler_trait = trait_additrait_belligerent_rul
			}
			admiral = {
				add_ruler_trait = trait_additrait_belligerent_rul
				}
			}
		}
	}
}

country_event = {
	id = addtrait_leader.7
	is_triggered_only = yes
	hide_window = yes

	trigger = {
			from.species = { has_trait = trait_additrait_pioneers }
	}

	immediate = {
		from = {
		switch = {
			trigger = leader_class
			ruler = {
				add_ruler_trait = trait_additrait_pioneers_rul
			}
			scientist = {
				add_ruler_trait = trait_additrait_pioneers_rul
			}
			governor = {
				add_ruler_trait = trait_additrait_pioneers_rul
			}
			general = {
				add_ruler_trait = trait_additrait_pioneers_rul
			}
			admiral = {
				add_ruler_trait = trait_additrait_pioneers_rul
				}
			}
		}
	}
}

country_event = {
	id = addtrait_leader.8
	is_triggered_only = yes
	hide_window = yes

	trigger = {
			from.species = { has_trait = trait_additrait_rooted }
	}

	immediate = {
		from = {
		switch = {
			trigger = leader_class
			ruler = {
				add_ruler_trait = trait_additrait_rooted_rul
			}
			scientist = {
				add_ruler_trait = trait_additrait_rooted_rul
			}
			governor = {
				add_ruler_trait = trait_additrait_rooted_rul
			}
			general = {
				add_ruler_trait = trait_additrait_rooted_rul
			}
			admiral = {
				add_ruler_trait = trait_additrait_rooted_rul
				}
			}
		}
	}
}

country_event = {
	id = addtrait_leader.9
	is_triggered_only = yes
	hide_window = yes

	trigger = {
			from.species = { has_trait = trait_additrait_influential }
	}
	
	immediate = {
		from = {
		switch = {
			trigger = leader_class
			ruler = {
				add_ruler_trait = trait_additrait_influential_rul
			}
			scientist = {
				add_ruler_trait = trait_additrait_influential_rul
			}
			governor = {
				add_ruler_trait = trait_additrait_influential_rul
			}
			general = {
				add_ruler_trait = trait_additrait_influential_rul
			}
			admiral = {
				add_ruler_trait = trait_additrait_influential_rul
				}
			}
		}
	}
}

country_event = {
	id = addtrait_leader.10
	is_triggered_only = yes
	hide_window = yes

	trigger = {
			from.species = { has_trait = trait_additrait_planetary_designers }
	}
	
	immediate = {
		from = {
		switch = {
			trigger = leader_class
			ruler = {
				add_ruler_trait = trait_additrait_planetary_designers_rul
			}
			scientist = {
				add_ruler_trait = trait_additrait_planetary_designers_rul
			}
			governor = {
				add_ruler_trait = trait_additrait_planetary_designers_rul
			}
			general = {
				add_ruler_trait = trait_additrait_planetary_designers_rul
			}
			admiral = {
				add_ruler_trait = trait_additrait_planetary_designers_rul
				}
			}
		}
	}
}

country_event = {
	id = addtrait_leader.11
	is_triggered_only = yes
	hide_window = yes

	trigger = {
			from.species = { has_trait = trait_additrait_planetary_gardeners }
	}

	immediate = {
		from = {
		switch = {
			trigger = leader_class
			ruler = {
				add_ruler_trait = trait_additrait_planetary_gardeners_rul
			}
			scientist = {
				add_ruler_trait = trait_additrait_planetary_gardeners_rul
			}
			governor = {
				add_ruler_trait = trait_additrait_planetary_gardeners_rul
			}
			general = {
				add_ruler_trait = trait_additrait_planetary_gardeners_rul
			}
			admiral = {
				add_ruler_trait = trait_additrait_planetary_gardeners_rul
				}
			}
		}
	}
}

country_event = {
	id = addtrait_leader.12
	is_triggered_only = yes
	hide_window = yes

	trigger = {
			from.species = { has_trait = trait_additrait_sky_born }
	}

    immediate = {
		from = {
			if = {
				limit = { leader_class = admiral }
				add_trait = trait_additrait_sky_born_adm
			}
		}
	}
}

country_event = {
	id = addtrait_leader.13
	is_triggered_only = yes
	hide_window = yes

	trigger = {
			from.species = { has_trait = trait_additrait_mob_mentality }
	}

	immediate = {
		from = {
		switch = {
			trigger = leader_class
			ruler = {
				add_ruler_trait = trait_additrait_mob_mentality_rul
			}
			scientist = {
				add_ruler_trait = trait_additrait_mob_mentality_rul
			}
			governor = {
				add_ruler_trait = trait_additrait_mob_mentality_rul
			}
			general = {
				add_ruler_trait = trait_additrait_mob_mentality_rul
			}
			admiral = {
				add_ruler_trait = trait_additrait_mob_mentality_rul
				}
			}
		}
	}
}
