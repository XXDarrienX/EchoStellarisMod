namespace = naturalsel_leaders

country_event = {
	id = naturalsel_leaders.1
	is_triggered_only = yes
	hide_window = yes

	trigger = {
			from.species = { has_trait = trait_predatory }
	}
	
	immediate = {
		from = {
		switch = {
			trigger = leader_class
			ruler = {
				add_ruler_trait = trait_predatory_rul
			}
			scientist = {
				add_ruler_trait = trait_predatory_rul
			}
			governor = {
				add_ruler_trait = trait_predatory_rul
			}
			general = {
				add_ruler_trait = trait_predatory_rul
			}
			admiral = {
				add_ruler_trait = trait_predatory_rul
				}
			}
		}
	}	
}

country_event = {
	id = naturalsel_leaders.2
	is_triggered_only = yes
	hide_window = yes

	trigger = {
			from.species = { has_trait = trait_evolved_brains }
	}

	immediate = {
		from = {
		switch = {
			trigger = leader_class
			ruler = {
				add_ruler_trait = trait_evolved_brains_rul
			}
			scientist = {
				add_ruler_trait = trait_evolved_brains_rul
			}
			governor = {
				add_ruler_trait = trait_evolved_brains_rul
			}
			general = {
				add_ruler_trait = trait_evolved_brains_rul
			}
			admiral = {
				add_ruler_trait = trait_evolved_brains_rul
				}
			}	
		}
	}
}

country_event = {
	id = naturalsel_leaders.3
	is_triggered_only = yes
	hide_window = yes

	trigger = {
			from.species = { has_trait = trait_claustrophobic }
	}
	
	immediate = {
		from = {
		switch = {
			trigger = leader_class
			ruler = {
				add_ruler_trait = trait_claustrophobic_rul
			}
			scientist = {
				add_ruler_trait = trait_claustrophobic_rul
			}
			governor = {
				add_ruler_trait = trait_claustrophobic_rul
			}
			general = {
				add_ruler_trait = trait_claustrophobic_rul
			}
			admiral = {
				add_ruler_trait = trait_claustrophobic_rul
				}
			}
		}	
	}
}

country_event = {
	id = naturalsel_leaders.4
	is_triggered_only = yes
	hide_window = yes

	trigger = {
			from.species = { has_trait = trait_agoraphobic }
	}
	immediate = {
		from = {
		switch = {
			trigger = leader_class
			ruler = {
				add_ruler_trait = trait_agoraphobic_rul
			}
			scientist = {
				add_ruler_trait = trait_agoraphobic_rul
			}
			governor = {
				add_ruler_trait = trait_agoraphobic_rul
			}
			general = {
				add_ruler_trait = trait_agoraphobic_rul
			}
			admiral = {
				add_ruler_trait = trait_agoraphobic_rul
				}
			}
		}
	}
}

country_event = {
	id = naturalsel_leaders.5
	is_triggered_only = yes
	hide_window = yes

	trigger = {
			from.species = { has_trait = trait_docile }
	}

	immediate = {
		from = {
		switch = {
			trigger = leader_class
			ruler = {
				add_ruler_trait = trait_docile_rul
			}
			scientist = {
				add_ruler_trait = trait_docile_rul
			}
			governor = {
				add_ruler_trait = trait_docile_rul
			}
			general = {
				add_ruler_trait = trait_docile_rul
			}
			admiral = {
				add_ruler_trait = trait_docile_rul
				}
			}
		}
	}
}