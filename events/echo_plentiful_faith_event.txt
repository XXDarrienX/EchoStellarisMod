namespace = plentiful_faith_indoctrination

planet_event = {
	id = plentiful_faith_indoctrination.1
	hide_window = yes
	is_triggered_only = yes
	
	immediate = {
        every_pop = {
            limit = { has_modifier = pop_recently_conquered }
            remove_modifier = pop_recently_conquered
        }
    }
}