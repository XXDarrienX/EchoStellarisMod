namespace = underworld_realms_origin

# ALL modded origins
event = {
	id = underworld_realms_origin.1
	hide_window = yes
	
	is_triggered_only = yes
	
	immediate = {
		
		# Origin : Touched by the Shroud
		every_country = {
			limit = {
				has_origin = origin_psychic
			}
			
			country_event = {id = underworld_realms_origin.2 }
		}
	}
}

# Origin : Touched by the Shroud
country_event = {
	id = underworld_realms_origin.2
	hide_window = yes

	is_triggered_only = yes

	immediate = {
		give_technology = { message = no tech = tech_psionic_theory }
	}
}