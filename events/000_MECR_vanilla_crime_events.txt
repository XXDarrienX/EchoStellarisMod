namespace = crime
# 官员腐败：增加帝国治理特殊效果
planet_event = {
	id = crime.2
	title = crime.2.name
	desc = {
		text = "crime.2.desc"
		trigger = { NOT = { has_building = building_imperial_government } }
	}
	desc = {
		text = "crime.2.desc.imperial_government"
		trigger = { has_building = building_imperial_government }
	}
	picture = GFX_evt_dark_alley
	show_sound = event_criminal_activity
	location = root
	is_triggered_only = yes

	pre_triggers = {
		has_owner = yes
	}

	trigger = {
		has_modifier = criminal_underworld
	}

	immediate = {
		add_modifier = {
			modifier = bribed_officials
			clear_on_owner_change = yes
			days = 3600
		}
		set_timed_planet_flag = {
			flag = planet_crime_flag
			days = 3600
		}
	}

	option = {
		name = crime.2.a
		trigger = { NOT = { has_building = building_imperial_government } }
		tooltip = {
			add_modifier = { modifier = bribed_officials days = 3600 }
		}
	}
	option = {
		name = crime.2.a.imperial_government
		trigger = { has_building = building_imperial_government }
		tooltip = {
			add_modifier = { modifier = bribed_officials days = 3600 }
		}
	}
}