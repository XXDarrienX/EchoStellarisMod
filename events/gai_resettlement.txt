namespace = gai_resettlement

event = {
	id = gai_resettlement.1	
	hide_window = yes
	is_triggered_only = yes
	
	immediate = {
		every_playable_country = {
			limit = {
				is_ai = yes
				is_country_type = "default"
				NOT = { has_policy_flag = resettlement_not_allowed }
			}
			country_event = { id = gai_resettlement.2 }
		}
	}
}

country_event = {
	id = gai_resettlement.2
	hide_window = yes
	is_triggered_only = yes
	
	immediate = {
		IF = { 
			limit = {
				any_owned_planet = { 
					is_planet_class = pc_habitat
					free_housing > 0
				}
			}
			random_owned_planet = {
				limit = {
					is_planet_class = pc_habitat
					free_housing > 0
				}
				planet_event = { id = gai_resettlement.8 }
			}
		}
		ELSE_IF = {
			limit = {
				any_owned_planet = {
					any_owned_pop = {
						OR = { 
							is_unemployed = yes
							has_job = servant
						}
						is_sapient = yes
					}
					is_colony = yes
					is_under_colonization = no
					has_orbital_bombardment = no
					has_building_construction = no
				}
			}
			random_owned_planet = {
				limit = { 
					has_orbital_bombardment = no
					is_colony = yes
					free_jobs > 0
					free_housing > 0
					is_under_colonization = no
					NOT = { any_owned_pop = { is_unemployed = yes } }
				}
				planet_event = { id = gai_resettlement.6 }
			}
			break = yes
		}
		ELSE_IF = {
			limit = {
				is_machine_empire = yes
				any_owned_planet = { 
					count_pops = { limit = { is_sapient = yes } count < 13 }
					is_colony = yes
					is_under_colonization = no
					has_orbital_bombardment = no
				}
			}
			IF = {
				LIMIT = {
					any_owned_planet = { 
						count_pops = { limit = { is_sapient = yes } count = 1 }
						is_colony = yes
						is_under_colonization = no
						has_orbital_bombardment = no
					}
				}
				random_owned_planet = {
					limit = { 
						count_pops = { limit = { is_sapient = yes } count = 1 }
						is_colony = yes
						is_under_colonization = no
						has_orbital_bombardment = no
					}
					planet_event = { id = gai_resettlement.3 }
				}
				break = yes
			}
			ELSE_IF = {
				LIMIT = {
					any_owned_planet = { 
						count_pops = { limit = { is_sapient = yes } count = 12 }
						is_colony = yes
						is_under_colonization = no
						has_orbital_bombardment = no
						free_housing > 0
					}
				}
				random_owned_planet = {
					limit = { 
						count_pops = { limit = { is_sapient = yes } count = 12 }
						is_colony = yes
						is_under_colonization = no
						has_orbital_bombardment = no
						free_housing > 0
					}
					planet_event = { id = gai_resettlement.3 }
				}
				break = yes
			}
			ELSE_IF = {
				LIMIT = {
					any_owned_planet = { 
						count_pops = { limit = { is_sapient = yes } count = 11 }
						is_colony = yes
						is_under_colonization = no
						has_orbital_bombardment = no
						free_housing > 0
					}
				}
				random_owned_planet = {
					limit = { 
						count_pops = { limit = { is_sapient = yes } count = 11 }
						is_colony = yes
						is_under_colonization = no
						has_orbital_bombardment = no
						free_housing > 0
					}
					planet_event = { id = gai_resettlement.3 }
				}
				break = yes
			}
			ELSE_IF = {
				LIMIT = {
					any_owned_planet = { 
						count_pops = { limit = { is_sapient = yes } count = 10 }
						is_colony = yes
						is_under_colonization = no
						has_orbital_bombardment = no
						free_housing > 0
					}
				}
				random_owned_planet = {
					limit = { 
						count_pops = { limit = { is_sapient = yes } count = 10 }
						is_colony = yes
						is_under_colonization = no
						has_orbital_bombardment = no
						free_housing > 0
					}
					planet_event = { id = gai_resettlement.3 }
				}
				break = yes
			}
			ELSE_IF = {
				LIMIT = {
					any_owned_planet = { 
						count_pops = { limit = { is_sapient = yes } count = 9 }
						is_colony = yes
						is_under_colonization = no
						has_orbital_bombardment = no
						free_housing > 0
					}
				}
				random_owned_planet = {
					limit = { 
						count_pops = { limit = { is_sapient = yes } count = 9 }
						is_colony = yes
						is_under_colonization = no
						has_orbital_bombardment = no
						free_housing > 0
					}
					planet_event = { id = gai_resettlement.3 }
				}
				break = yes
			}
			ELSE_IF = {
				LIMIT = {
					any_owned_planet = { 
						count_pops = { limit = { is_sapient = yes } count = 8 }
						is_colony = yes
						is_under_colonization = no
						has_orbital_bombardment = no
						free_housing > 0
					}
				}
				random_owned_planet = {
					limit = { 
						count_pops = { limit = { is_sapient = yes } count = 8 }
						is_colony = yes
						is_under_colonization = no
						has_orbital_bombardment = no
						free_housing > 0
					}
					planet_event = { id = gai_resettlement.3 }
				}
				break = yes
			}
			ELSE_IF = {
				LIMIT = {
					any_owned_planet = { 
						count_pops = { limit = { is_sapient = yes } count = 7 }
						is_colony = yes
						is_under_colonization = no
						has_orbital_bombardment = no
						free_housing > 0
					}
				}
				random_owned_planet = {
					limit = { 
						count_pops = { limit = { is_sapient = yes } count = 7 }
						is_colony = yes
						is_under_colonization = no
						has_orbital_bombardment = no
						free_housing > 0
					}
					planet_event = { id = gai_resettlement.3 }
				}
				break = yes
			}
			ELSE_IF = {
				LIMIT = {
					any_owned_planet = { 
						count_pops = { limit = { is_sapient = yes } count = 6 }
						is_colony = yes
						is_under_colonization = no
						has_orbital_bombardment = no
						free_housing > 0
					}
				}
				random_owned_planet = {
					limit = { 
						count_pops = { limit = { is_sapient = yes } count = 6 }
						is_colony = yes
						is_under_colonization = no
						has_orbital_bombardment = no
						free_housing > 0
					}
					planet_event = { id = gai_resettlement.3 }
				}
				break = yes
			}
			ELSE_IF = {
				LIMIT = {
					any_owned_planet = { 
						count_pops = { limit = { is_sapient = yes } count = 5 }
						is_colony = yes
						is_under_colonization = no
						has_orbital_bombardment = no
						free_housing > 0
					}
				}
				random_owned_planet = {
					limit = { 
						count_pops = { limit = { is_sapient = yes } count = 5 }
						is_colony = yes
						is_under_colonization = no
						has_orbital_bombardment = no
						free_housing > 0
					}
					planet_event = { id = gai_resettlement.3 }
				}
				break = yes
			}
			ELSE_IF = {
				LIMIT = {
					any_owned_planet = { 
						count_pops = { limit = { is_sapient = yes } count = 4 }
						is_colony = yes
						is_under_colonization = no
						has_orbital_bombardment = no
					}
				}
				random_owned_planet = {
					limit = { 
						count_pops = { limit = { is_sapient = yes } count = 4 }
						is_colony = yes
						is_under_colonization = no
						has_orbital_bombardment = no
					}
					planet_event = { id = gai_resettlement.3 }
				}
				break = yes
			}
			ELSE_IF = {
				LIMIT = {
					any_owned_planet = { 
						count_pops = { limit = { is_sapient = yes } count = 3 }
						is_colony = yes
						is_under_colonization = no
						has_orbital_bombardment = no
					}
				}
				random_owned_planet = {
					limit = { 
						count_pops = { limit = { is_sapient = yes } count = 3 }
						is_colony = yes
						is_under_colonization = no
						has_orbital_bombardment = no
					}
					planet_event = { id = gai_resettlement.3 }
				}
				break = yes
			}
			ELSE_IF = {
				LIMIT = {
					any_owned_planet = { 
						count_pops = { limit = { is_sapient = yes } count = 2 }
						is_colony = yes
						is_under_colonization = no
						has_orbital_bombardment = no
					}
				}
				random_owned_planet = {
					limit = { 
						count_pops = { limit = { is_sapient = yes } count = 2 }
						is_colony = yes
						is_under_colonization = no
						has_orbital_bombardment = no
					}
					planet_event = { id = gai_resettlement.3 }
				}
				break = yes
			}
		}
		ELSE_IF = {
			limit = {
				is_machine_empire = no
				any_owned_planet = { 
					count_pops = { limit = { is_sapient = yes } count < 10 }
					is_colony = yes
					is_under_colonization = no
					has_orbital_bombardment = no
				}
			}
			IF = {
				LIMIT = {
					any_owned_planet = { 
						count_pops = { limit = { is_sapient = yes } count = 9 }
						is_colony = yes
						is_under_colonization = no
						has_orbital_bombardment = no
						free_housing > 0
					}
				}
				random_owned_planet = {
					limit = { 
						count_pops = { limit = { is_sapient = yes } count = 9 }
						is_colony = yes
						is_under_colonization = no
						has_orbital_bombardment = no
						free_housing > 0
					}
					planet_event = { id = gai_resettlement.7 }
				}
				break = yes
			}
			ELSE_IF = {
				LIMIT = {
					any_owned_planet = { 
						count_pops = { limit = { is_sapient = yes } count = 8 }
						is_colony = yes
						is_under_colonization = no
						has_orbital_bombardment = no
						free_housing > 0
					}
				}
				random_owned_planet = {
					limit = { 
						count_pops = { limit = { is_sapient = yes } count = 8 }
						is_colony = yes
						is_under_colonization = no
						has_orbital_bombardment = no
						free_housing > 0
					}
					planet_event = { id = gai_resettlement.7 }
				}
				break = yes
			}
			ELSE_IF = {
				LIMIT = {
					any_owned_planet = { 
						count_pops = { limit = { is_sapient = yes } count = 7 }
						is_colony = yes
						is_under_colonization = no
						has_orbital_bombardment = no
						free_housing > 0
					}
				}
				random_owned_planet = {
					limit = { 
						count_pops = { limit = { is_sapient = yes } count = 7 }
						is_colony = yes
						is_under_colonization = no
						has_orbital_bombardment = no
						free_housing > 0
					}
					planet_event = { id = gai_resettlement.7 }
				}
				break = yes
			}
			ELSE_IF = {
				LIMIT = {
					any_owned_planet = { 
						count_pops = { limit = { is_sapient = yes } count = 6 }
						is_colony = yes
						is_under_colonization = no
						has_orbital_bombardment = no
						free_housing > 0
					}
				}
				random_owned_planet = {
					limit = { 
						count_pops = { limit = { is_sapient = yes } count = 6 }
						is_colony = yes
						is_under_colonization = no
						has_orbital_bombardment = no
						free_housing > 0
					}
					planet_event = { id = gai_resettlement.7 }
				}
				break = yes
			}
			ELSE_IF = {
				LIMIT = {
					any_owned_planet = { 
						count_pops = { limit = { is_sapient = yes } count = 5 }
						is_colony = yes
						is_under_colonization = no
						has_orbital_bombardment = no
						free_housing > 0
					}
				}
				random_owned_planet = {
					limit = { 
						count_pops = { limit = { is_sapient = yes } count = 5 }
						is_colony = yes
						is_under_colonization = no
						has_orbital_bombardment = no
						free_housing > 0
					}
					planet_event = { id = gai_resettlement.7 }
				}
				break = yes
			}
			ELSE_IF = {
				LIMIT = {
					any_owned_planet = { 
						count_pops = { limit = { is_sapient = yes } count = 4 }
						is_colony = yes
						is_under_colonization = no
						has_orbital_bombardment = no
					}
				}
				random_owned_planet = {
					limit = { 
						count_pops = { limit = { is_sapient = yes } count = 4 }
						is_colony = yes
						is_under_colonization = no
						has_orbital_bombardment = no
					}
					planet_event = { id = gai_resettlement.7 }
				}
				break = yes
			}
			ELSE_IF = {
				LIMIT = {
					any_owned_planet = { 
						count_pops = { limit = { is_sapient = yes } count = 3 }
						is_colony = yes
						is_under_colonization = no
						has_orbital_bombardment = no
					}
				}
				random_owned_planet = {
					limit = { 
						count_pops = { limit = { is_sapient = yes } count = 3 }
						is_colony = yes
						is_under_colonization = no
						has_orbital_bombardment = no
					}
					planet_event = { id = gai_resettlement.7 }
				}
				break = yes
			}
			ELSE_IF = {
				LIMIT = {
					any_owned_planet = { 
						count_pops = { limit = { is_sapient = yes } count = 2 }
						is_colony = yes
						is_under_colonization = no
						has_orbital_bombardment = no
					}
				}
				random_owned_planet = {
					limit = { 
						count_pops = { limit = { is_sapient = yes } count = 2 }
						is_colony = yes
						is_under_colonization = no
						has_orbital_bombardment = no
					}
					planet_event = { id = gai_resettlement.7 }
				}
				break = yes
			}
			ELSE_IF = {
				LIMIT = {
					any_owned_planet = { 
						count_pops = { limit = { is_sapient = yes } count = 1 }
						is_colony = yes
						is_under_colonization = no
						has_orbital_bombardment = no
					}
				}
				random_owned_planet = {
					limit = { 
						count_pops = { limit = { is_sapient = yes } count = 1 }
						is_colony = yes
						is_under_colonization = no
						has_orbital_bombardment = no
					}
					planet_event = { id = gai_resettlement.7 }
				}
				break = yes
			}
		}
		ELSE_IF = {
			limit = {
				any_owned_planet = {
					free_jobs > 0
					has_orbital_bombardment = no
					is_colony = yes
					is_under_colonization = no
					OR = {
						#gai_good_food = yes
						gai_good_minerals = yes
						#gai_good_energy = yes
					}
				}
			}
			random_owned_planet = {
				limit = { 
					free_jobs > 0
					is_under_colonization = no
					has_orbital_bombardment = no
					is_colony = yes
					is_under_colonization = no
					OR = {
						#gai_good_food = yes
						gai_good_minerals = yes
						#gai_good_energy = yes
					}
				}
				planet_event = { id = gai_resettlement.4 }
			}
			break = yes
		}
		ELSE_IF = {
			limit = {
				years_passed > 20
				any_owned_planet = {
					has_planet_flag = gai_building_up
					OR = { 
						free_district_slots > 0
						free_jobs > 4
					}
					is_colony = yes
					free_jobs > 0
					has_orbital_bombardment = no
				}
			}
			random_owned_planet = {
				limit = {
					has_planet_flag = gai_building_up
					free_district_slots > 0
					is_colony = yes
					free_jobs > 0
					has_orbital_bombardment = no
				}
				planet_event = { id = gai_resettlement.5 }
			}
			break = yes
		}
		ELSE_IF = {
			limit = {
				years_passed > 20
				any_owned_planet = {
					count_pops = { limit = { is_sapient = yes } count > 11 }
					NOT = { has_planet_flag = gai_building_up }
					OR = { 
						free_district_slots > 0
						free_jobs > 4
					}
					is_colony = yes
					free_jobs > 0
					has_orbital_bombardment = no
				}
			}
			random_owned_planet = {
				limit = {
					count_pops = { limit = { is_sapient = yes } count > 11 }
					free_district_slots > 0
					is_colony = yes
					free_jobs > 0
					has_orbital_bombardment = no
				}
				planet_event = { id = gai_resettlement.5 }
			}
		}
	}
}

planet_event = {
	id = gai_resettlement.3
	hide_window = yes
	is_triggered_only = yes
	
	immediate = {
		set_timed_planet_flag = { flag = not_this_planet_lol days = 3 }
		owner = {
			random_owned_planet = {
				limit = {
					has_orbital_bombardment = no
					NOT = { has_planet_flag = not_this_planet_lol }
					NOT = { is_capital = yes }
					NOR = {
						count_pops = { limit = { is_sapient = yes } count = 1 }
						count_pops = { limit = { is_sapient = yes } count = 2 }
						AND = {
							count_pops = { limit = { is_sapient = yes } count < 13 }
							count_pops = { limit = { is_sapient = yes } count > 4 }
						}
						count_pops = { limit = { is_sapient = yes } count = 15 }
						count_pops = { limit = { is_sapient = yes } count = 20 }
						count_pops = { limit = { is_sapient = yes } count = 25 }
						count_pops = { limit = { is_sapient = yes } count = 30 }
						count_pops = { limit = { is_sapient = yes } count = 35 }
						count_pops = { limit = { is_sapient = yes } count = 40 }
						count_pops = { limit = { is_sapient = yes } count = 45 }
						count_pops = { limit = { is_sapient = yes } count = 50 }
						count_pops = { limit = { is_sapient = yes } count = 55 }
						count_pops = { limit = { is_sapient = yes } count = 60 }
						count_pops = { limit = { is_sapient = yes } count = 65 }
						count_pops = { limit = { is_sapient = yes } count = 70 }
						count_pops = { limit = { is_sapient = yes } count = 75 }
					}
				}
				random_owned_pop = {
					limit = { is_sapient = yes }
					resettle_pop = {
						pop = this
						planet = root
					}
				}
			}
		}
		IF = { #Assimilators/Trophies
			limit = {
				planet = { count_pops = { limit = { is_sapient = yes } count < 3 } }
				owner = { 
					is_machine_empire = yes
					any_owned_pop = {
						is_robot_pop = no
						is_being_purged = no
						is_being_assimilated = no
					}
				}
			}
			owner = {
				random_owned_planet = {
					limit = {
						is_capital = yes
						any_owned_pop = { 
							is_organic_species = yes
						}
					}
					random_owned_pop = {
						limit = {
							is_organic_species = yes
							is_sapient = yes
						}
						resettle_pop = {
							pop = this
							planet = root
						}
					}
					random_owned_pop = {
						limit = {
							is_organic_species = no
							is_sapient = yes
						}
						resettle_pop = {
							pop = this
							planet = root
						}
					}
				}
			}
		}
	}
}

planet_event = {
	id = gai_resettlement.4
	hide_window = yes
	is_triggered_only = yes
	
	immediate = {
		set_timed_planet_flag = { flag = not_this_planet_lol days = 3 }
		owner = {
			random_owned_planet = {
				limit = {
					has_orbital_bombardment = no
					has_building_construction = no
					NAND = {  
						is_capital = yes 
						count_pops < 76
					}
					NOT = { has_planet_flag = not_this_planet_lol }
					NOR = {
						count_pops < 16
						count_pops = { limit = { is_sapient = yes } count = 20 }
						count_pops = { limit = { is_sapient = yes } count = 25 }
						count_pops = { limit = { is_sapient = yes } count = 30 }
						count_pops = { limit = { is_sapient = yes } count = 35 }
						count_pops = { limit = { is_sapient = yes } count = 40 }
						count_pops = { limit = { is_sapient = yes } count = 45 }
						count_pops = { limit = { is_sapient = yes } count = 50 }
						count_pops = { limit = { is_sapient = yes } count = 55 }
						count_pops = { limit = { is_sapient = yes } count = 60 }
						count_pops = { limit = { is_sapient = yes } count = 65 }
						count_pops = { limit = { is_sapient = yes } count = 70 }
						count_pops = { limit = { is_sapient = yes } count = 75 }
						gai_good_food = yes
						gai_good_minerals = yes
						gai_good_energy = yes
					}
					any_owned_pop = { 
						NOT = { has_citizenship_type = { type = citizenship_organic_trophy } }
						NOR = {
							is_pop_category = specialist
							is_pop_category = ruler
						}
					}
				}
				random_owned_pop = {
					limit = {
						is_sapient = yes
						NOR = {
							is_pop_category = specialist
							is_pop_category = ruler
						}
					}
					resettle_pop = {
						pop = this
						planet = root
					}
				}
			}
		}
	}
}

planet_event = {
	id = gai_resettlement.5
	hide_window = yes
	is_triggered_only = yes
	
	immediate = {
		remove_planet_flag = gai_building_up
		set_timed_planet_flag = { flag = not_this_planet_lol days = 3 }
		set_timed_planet_flag = { flag = gai_building_up days = 400 }
		owner = {
			random_owned_planet = {
				limit = {
					has_orbital_bombardment = no
					has_building_construction = no
					NAND = {  
						is_capital = yes 
						count_pops < 76
					}
					count_pops > 12
					free_district_slots > 0
					NOT = { has_planet_flag = not_this_planet_lol }
					NOT = { has_planet_flag = gai_building_up }
					any_owned_pop = {
						NOT = { has_citizenship_type = { type = citizenship_organic_trophy } }
						NOR = {
							is_pop_category = specialist
							is_pop_category = ruler
						}
					}
				}
				random_owned_pop = {
					limit = {
						is_sapient = yes
						NOT = { has_citizenship_type = { type = citizenship_organic_trophy } }
						NOR = {
							is_pop_category = specialist
							is_pop_category = ruler
						}
					}
					resettle_pop = {
						pop = this
						planet = root
					}
				}
			}
		}
	}
}

planet_event = {
	id = gai_resettlement.6
	hide_window = yes
	is_triggered_only = yes
	
	immediate = {
		set_timed_planet_flag = { flag = not_this_planet_lol days = 3 }
		owner = {
			random_owned_planet = {
				limit = {
					has_orbital_bombardment = no
					has_building_construction = no
					any_owned_pop = {
						OR = { 
							is_unemployed = yes
							has_job = servant
						}
						is_sapient = yes
					}
					NOT = { has_planet_flag = not_this_planet_lol }
					NOR = {
						count_pops = { limit = { is_sapient = yes } count < 13 }
						count_pops = { limit = { is_sapient = yes } count = 20 }
						count_pops = { limit = { is_sapient = yes } count = 25 }
						count_pops = { limit = { is_sapient = yes } count = 30 }
						count_pops = { limit = { is_sapient = yes } count = 35 }
						count_pops = { limit = { is_sapient = yes } count = 40 }
						count_pops = { limit = { is_sapient = yes } count = 45 }
						count_pops = { limit = { is_sapient = yes } count = 50 }
						count_pops = { limit = { is_sapient = yes } count = 55 }
						count_pops = { limit = { is_sapient = yes } count = 60 }
						count_pops = { limit = { is_sapient = yes } count = 65 }
						count_pops = { limit = { is_sapient = yes } count = 70 }
						count_pops = { limit = { is_sapient = yes } count = 75 }
					}
				}
				random_owned_pop = {
					limit = {
						is_sapient = yes
						OR = { 
							is_unemployed = yes
							has_job = servant
						}
					}
					resettle_pop = {
						pop = this
						planet = root
					}
				}
			}
		}
	}
}

planet_event = {
	id = gai_resettlement.7
	hide_window = yes
	is_triggered_only = yes
	
	immediate = {
		set_timed_planet_flag = { flag = not_this_planet_lol days = 3 }
		owner = {
			random_owned_planet = {
				limit = {
					has_orbital_bombardment = no
					NOT = { has_planet_flag = not_this_planet_lol }
					#NOT = { is_capital = yes }
					NOR = { 
						count_pops = { limit = { is_sapient = yes } count = 1 }
						count_pops = { limit = { is_sapient = yes } count = 5 }
						count_pops = { limit = { is_sapient = yes } count = 10 }
						count_pops = { limit = { is_sapient = yes } count = 15 }
						count_pops = { limit = { is_sapient = yes } count = 20 }
						count_pops = { limit = { is_sapient = yes } count = 25 }
						count_pops = { limit = { is_sapient = yes } count = 30 }
						count_pops = { limit = { is_sapient = yes } count = 35 }
						count_pops = { limit = { is_sapient = yes } count = 40 }
						count_pops = { limit = { is_sapient = yes } count = 45 }
						count_pops = { limit = { is_sapient = yes } count = 50 }
						count_pops = { limit = { is_sapient = yes } count = 55 }
						count_pops = { limit = { is_sapient = yes } count = 60 }
						count_pops = { limit = { is_sapient = yes } count = 65 }
						count_pops = { limit = { is_sapient = yes } count = 70 }
						count_pops = { limit = { is_sapient = yes } count = 75 }
					}
				}
				random_owned_pop = {
					limit = { 
						is_sapient = yes 
						NOR = {
							is_pop_category = specialist
							is_pop_category = ruler
						}
					}
					resettle_pop = {
						pop = this
						planet = root
					}
				}
			}
		}
	}
}

planet_event = {
	id = gai_resettlement.8
	hide_window = yes
	is_triggered_only = yes
	
	immediate = {
		set_timed_planet_flag = { flag = not_this_planet_lol days = 3 }
		owner = {
			random_owned_planet = {
				limit = {
					has_orbital_bombardment = no
					NOT = { has_planet_flag = not_this_planet_lol }
					NOT = { is_capital = yes }
					NOR = { 
						count_pops = { limit = { is_sapient = yes } count = 1 }
						count_pops = { limit = { is_sapient = yes } count = 5 }
						count_pops = { limit = { is_sapient = yes } count = 10 }
						count_pops = { limit = { is_sapient = yes } count = 15 }
						count_pops = { limit = { is_sapient = yes } count = 20 }
						count_pops = { limit = { is_sapient = yes } count = 25 }
						count_pops = { limit = { is_sapient = yes } count = 30 }
						count_pops = { limit = { is_sapient = yes } count = 35 }
						count_pops = { limit = { is_sapient = yes } count = 40 }
						count_pops = { limit = { is_sapient = yes } count = 45 }
						count_pops = { limit = { is_sapient = yes } count = 50 }
						count_pops = { limit = { is_sapient = yes } count = 55 }
						count_pops = { limit = { is_sapient = yes } count = 60 }
						count_pops = { limit = { is_sapient = yes } count = 65 }
						count_pops = { limit = { is_sapient = yes } count = 70 }
						count_pops = { limit = { is_sapient = yes } count = 75 }
					}
				}
				random_owned_pop = {
					limit = { 
						is_sapient = yes 
						NOR = {
							is_pop_category = specialist
							is_pop_category = ruler
						}
					}
					resettle_pop = {
						pop = this
						planet = root
					}
				}
			}
		}
	}
}