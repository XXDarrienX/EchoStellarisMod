namespace = mod_horde_capturing

planet_event = {
	id = mod_horde_capturing.1
	hide_window = yes
	is_triggered_only = yes
	trigger = {  
		From = { is_country_type = awakened_marauders }
	}	
	immediate = {
	    planet_event = { id = mod_horde_capturing.1 }
	}	
}

planet_event = {
	id = mod_horde_capturing.2
	hide_window = yes
	is_triggered_only = yes	
	immediate = {
	    if = { limit = { free_building_slots > 0 }
		    add_building = building_plentiful_traditions_zitadom        
		}
	    else_if = { limit = { has_building = building_stronghold }
		    remove_building = building_stronghold
		    add_building = building_fortress        
		}		
	    if = { limit = { free_building_slots > 0 }
		    add_building = building_mem_planetary_shield_generator_heavy       
		}		
	}	
}