namespace = mod_horde_capturing

event = {
	id = mod_horde_capturing.1
	hide_window = yes
	is_triggered_only = yes
	trigger = { 
	    is_difficulty > 2 
	}	
	immediate = {
	    every_country = {
		    limit = { is_country_type = awakened_marauders }
			every_owned_planet = {
				limit = {
					num_pops > 4
					NOT = { has_building = building_fortress }
					free_building_slots > 0
				}
				add_building = building_plentiful_traditions_zitadom
				if = { 
					limit = { 
						free_building_slots > 0 
						NOT = { has_building = building_planetary_shield_generator  }					
					}
					add_building = building_mem_planetary_shield_generator_heavy       
				}			
			}
		}
	}
}