#################################
#
# Envoy Events
# by Pierre du Plessis
#
#################################

@envoy_punishment_cooldown = 1080 #three years

namespace = envoy_events

#event_target offended_party is for operation_diplomatic_incident purposes via set_espionage_diplo_incident_success_flag. Safer than retrospectively trying to make all the event targets the same.

leader_event = {
	id = envoy_events.230
	title = envoy_events.230.name
	desc = envoy_events.230.desc
	picture = GFX_evt_cover_blown
	show_sound = event_bad_omen

	is_triggered_only = yes

	trigger = {
		leader_class = envoy
		exists = envoy_location_country
		has_leader_flag = fugitive_envoy@envoy_location_country
	}

	immediate = { if = { limit = { NOT = { exists = event_target:event_envoy } } save_event_target_as = event_envoy } } # vfix - "Unknown promotion `event_envoy` in text: event_envoy.GetName]! is removed from our leader pool." - events\envoy_events.txt (line 3618)
	option = {
		name = envoy_events.230.A

		custom_tooltip = envoy_events.225.A.tooltip
		hidden_effect = {
			kill_leader = {
				show_notification = no
				fire = yes
			}
		}
		owner = {
			if = {
				limit = {
					resource_stockpile_compare = {
						resource = influence
						value >= 30
					}
				}
				add_resource = {
					influence = -30
				}
			}
			else = {
				add_modifier = {
					modifier = galactic_community_diplo_corps_is_a_joke
					days = 3600
				}
			}
		}
	}
}
