########################
#
# Precursor Events
# Written by Henrik Thyrwall
#
########################

namespace = precursor

##################
# THE YUHT EMPIRE
##################

# Yuht Intro
# via on_survey = {} | this = surveying vessel
ship_event = {
	id = precursor.500
	title = "precursor.500.name"
	desc = "precursor.500.desc"
	picture = GFX_evt_archaeological_dig
	location = from
	event_chain = yuht_chain

	is_triggered_only = yes

	trigger = {
		exists = solar_system # vfix - "Invalid context switch [solar_system] from [ship], file: events/precursor_events.txt line: 2051"
		solar_system = {
			has_star_flag = precursor_2
			NOT = { is_same_value = root.owner.capital_scope.solar_system }
		}
		owner = {
			is_ai = no
			has_precursor_intro = no
		}
		FROM = {
			has_anomaly = no
			is_star = no
		}
	}
	weight_multiplier = {
		factor = 1
		modifier = {
			factor = @origin_shoulders_multiplier
			owner = {
				has_origin = origin_shoulders_of_giants
				has_country_flag = origin_shoulders_closure
			}
		}
	}

	immediate = {
		owner = {
			begin_event_chain = {
				event_chain = "yuht_chain"
				target = THIS
			}
			set_country_flag = yuht_intro
		}
	}

	option = {
		name = INTERESTING
		hidden_effect = { owner = { country_event = { id = story.5 days = 30 } } }
		owner = {
			tooltip = {
				begin_event_chain = {
					event_chain = "yuht_chain"
					target = THIS
				}
			}
			if = {
				limit = { has_ancrel = yes }
				small_artifact_reward = yes
			}
		}
	}
}