namespace = gwen_spaceborn_thing

country_event = {
	id = gwen_spaceborn_thing.1
	hide_window = yes
	is_triggered_only = yes
	
	trigger = { has_civic = civic_Spaceborn }
	immediate = {
			random_owned_planet = {
				limit = { is_capital = yes }
				change_pc = pc_ringworld_habitable
				set_planet_size = 25
				reroll_deposits = yes
				reset_start_buildings_and_blockers_life_seeded = yes
				clear_blockers = yes
			}
			every_owned_pop = {
				modify_species = {
					species = this
					add_trait = trait_spaceborn_habitability
					ideal_planet_class = pc_habitat
					base = none 
				}
			}
			change_dominant_species = { species = last_created change_all = yes }
		}
	}
